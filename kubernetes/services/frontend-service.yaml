apiVersion: v1
kind: Service
metadata:
  name: frontend-service  # Changed back to frontend-service to match Nginx config!
  labels:
    app: frontend
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000